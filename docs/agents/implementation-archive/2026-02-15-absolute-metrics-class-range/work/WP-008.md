# WP-008: Manifest Documentation Updates

## Overview

Update all relevant GUI project manifest documents to reflect the new files, APIs, data flows, patterns, and types introduced across WP-001 through WP-007.

## Plan Reference

**Manifest update requirement** referenced throughout the plan's "Required Components → Manifest Documents to Update" section.

## Description

The GUI project manifest must remain synchronized with code changes. This WP ensures that all new services, DTOs, endpoints, hooks, components, and utility functions are documented in the appropriate manifest files. This is a documentation-only work package.

## Requirements

### Update `gui/docs/project-manifest/public-api.md`
- Add `ClassRangeService` with `calculateClassRange()` method signature.
- Add `ClassRangeEndpoint` with `calculate()` method signature.
- Add DTOs: `ClassRangeRequest`, `ClassRangeResponse`, `RangeMetric`, `ShipMetricSummary` with all fields.
- Update `PhysicsService` method signatures to reflect new parameters.
- Update `ShipDataService` method signatures.
- Update `PhysicsRequest`, `PhysicsResponse`, `EnginePerformance`, `ShipDetails` DTO fields.
- Add `useClassRange` hook signature.
- Add new component signatures: `RangeBar`, `AbsoluteMetricCard`, `WorstCaseCard`, `ClassRangePanel`.
- Add `metricContext.ts` utility function signatures.

### Update `gui/docs/project-manifest/file-tree.md`
- Add all new files:
  - Backend: 4 DTOs, 1 service, 1 endpoint
  - Frontend: 1 hook, 4 components, 1 utility
- Ensure directory structure reflects reality.

### Update `gui/docs/project-manifest/data-flows.md`
- Add "Class-Range Calculation Flow" diagram showing:
  - User adjusts sliders → `useClassRange` hook fires → `POST /api/calculate/class-range` → `ClassRangeService` iterates ships → aggregates results → `ClassRangeResponse` → `ClassRangePanel` renders
- Update existing "Single-Ship Physics Calculation Flow" to reflect real data usage.

### Update `gui/docs/project-manifest/tech-stack.md`
- Add "Class-Wide Aggregation" as a new architectural pattern.
- Update "Service Layer" pattern documentation if needed.

### Update `gui/docs/project-manifest/constraints.md`
- Add any new constraints that emerged (if applicable).
- Document the 500ms debounce for class-range vs 300ms for single-ship.

### Update `gui/docs/API.md`
- Add `POST /api/calculate/class-range` endpoint documentation.
- Document request/response format with examples.

## Technical Constraints

- Documentation only — no code changes.
- Must accurately reflect the final state of code after WP-001 through WP-007.
- Follow existing manifest documentation style and formatting.

## Files Modified

- `gui/docs/project-manifest/public-api.md`
- `gui/docs/project-manifest/file-tree.md`
- `gui/docs/project-manifest/data-flows.md`
- `gui/docs/project-manifest/tech-stack.md`
- `gui/docs/project-manifest/constraints.md` (if applicable)
- `gui/docs/API.md`

## Files Created

None.

## Dependencies

- **WP-001** through **WP-007** — All code changes must be complete before documentation can accurately reflect the final state.

## Acceptance Criteria

1. `public-api.md` documents all new and modified classes, methods, DTOs, hooks, and components.
2. `file-tree.md` includes all new files in their correct directory locations.
3. `data-flows.md` includes a class-range calculation flow diagram.
4. `tech-stack.md` documents the class-wide aggregation pattern.
5. `API.md` documents the new `POST /api/calculate/class-range` endpoint with request/response examples.
6. No contradictions between manifest and code.
7. All "Last Updated" dates are refreshed in affected documents.
